# DESIGN.md — PSU ALS Frontend

มาตรฐาน UI ของ `G06_adaptive_learning` ทุกค่าในเอกสารนี้วัดจากโค้ดจริงและผ่าน WCAG AA แล้ว
ถ้าจะเพิ่มสี ขนาด หรือรัศมีที่ไม่อยู่ในนี้ ให้แก้เอกสารนี้ก่อน แล้วค่อยแก้โค้ด

---

## 0. หลักการ

1. **หนึ่งความหมาย หนึ่งชื่อ token** — สีเดียวกันต้องเรียกเหมือนกันทุกไฟล์
2. **navy คือสีของ "สิ่งที่สำคัญที่สุดในจอ"** — ไม่ใช่สีของทุกอย่าง
3. **ตัวอักษรทุกตัวต้องผ่าน AA (4.5:1)** ไม่มีข้อยกเว้น ไม่มีตัวอักษรตกแต่งที่อ่านไม่ออก
4. **ตัวควบคุมใช้สีทึบ** gradient สงวนไว้ให้พื้นหลังตกแต่งที่กดไม่ได้เท่านั้น
5. **ไม่มีค่าดิบในไฟล์ component** — ห้ามเขียน hex ห้ามเขียนรัศมีที่ไม่อยู่ในสเกล

---

## 1. Token — หนึ่ง palette หนึ่งชุดชื่อ

ชุดชื่อที่ถูกต้องคือชุดของ `Home.css` / `Adminhome.css` (ครอบคลุม 3,175 จาก 7,338 บรรทัดของ CSS ทั้งโปรเจกต์)

### 1.1 พื้นผิว

| token | light | dark | ใช้กับ |
|---|---|---|---|
| `--bg` | `#f2f7fa` | `#0b1220` | พื้นหน้าจอ |
| `--surface` | `#ffffff` | `#121b2e` | การ์ด modal ตาราง |
| `--surface-2` | `#f8fafc` | `#172238` | พื้นรองในการ์ด ปุ่มยกเลิก |
| `--tint` | `#e8f0fe` | `#1a2a4a` | แถว hover แถว active hero |
| `--overlay` | `rgba(0,40,120,.18)` | `rgba(2,6,16,.6)` | ฉากหลัง modal |

### 1.2 ตัวอักษร — 3 ระดับ

| token | light | บน `--surface` | dark | ใช้กับ |
|---|---|---|---|---|
| `--text` | `#000080` navy | **16.01:1** | `#ffffff` | หัวข้อ ชื่อรายการ — จุดที่ต้องมองก่อน |
| `--ink` | `#334155` | **10.35:1** | `#cbd5e1` | เนื้อความ ข้อมูลในเซลล์ |
| `--muted` | `#475569` | **7.58:1** | `#a3b1cc` | หัวตาราง label caption placeholder |

**เลิกใช้:** `--ink-soft` `--faint` `--muted2` `--border2` → ทั้งหมดชี้ไป `--muted`

สามระดับพอแล้ว ที่ผ่านมามี 5 ระดับแต่ไม่มีใครรู้ว่าตัวไหนใช้เมื่อไหร่ จึงกลายเป็นว่าเลือกตามใจ

> ค่าเดิมที่ถูกทิ้งเพราะตกมาตรฐาน: `#6D8196` = **3.72:1** (เคยถูกใช้ราว 110 จุด) และ `#94a3b8` = **2.38:1**
> dark mode ไม่เคยพัง — `#a3b1cc` = 7.95:1 มาตั้งแต่แรก ปัญหาอยู่ที่ light mode อย่างเดียว

### 1.3 สีแบรนด์

| token | light | dark | ใช้กับ |
|---|---|---|---|
| `--accent` | `#0047AB` | `#6b9bff` | ตัวอักษร ไอคอน ขอบ ที่เป็นสีแบรนด์ |
| `--accent-fill` | `#0047AB` | `#2f5fd0` | **พื้นทึบที่มีตัวอักษรขาวทับ** |
| `--on-accent` | `#ffffff` | `#ffffff` | ตัวอักษรบน `--accent-fill` |
| `--accent2` | `#82C8E5` | `#82C8E5` | ประดับเท่านั้น — **ห้ามใช้เป็นพื้นของตัวอักษร** |

`--accent2` กับตัวอักษรขาวได้ **1.85:1** ตกมาตรฐานชัดเจน ใช้ได้เฉพาะเส้นคั่น ไอคอนประดับ และพื้นหลัง blur

### 1.4 สีสถานะ

| token | light | dark |
|---|---|---|
| `--green` | `#10b981` | `#34d399` |
| `--red` | `#dc2626` | `#f87171` |
| `--orange` | `#f59e0b` | `#fbbf24` |
| `--blue` | `#3b82f6` | `#60a5fa` |
| `--purple` | `#8b5cf6` | `#a78bfa` |

คู่พื้น/ตัวอักษร/ขอบของ badge ใช้ `--ok-*` `--mid-*` `--bad-*` `--warn-*` ตามที่ `Adminhome.css` ประกาศไว้แล้ว

### 1.5 ตารางแปลงชื่อเก่า

ชื่อฝั่งซ้ายต้องหายไปจากโปรเจกต์

| ชื่อเก่า | ไฟล์ที่ใช้ | ชื่อที่ถูก |
|---|---|---|
| `--navy` | Pretest, InformationForm, GetStart | `--bg` |
| `--navy2` | Pretest, InformationForm, GetStart | `--surface` |
| `--navy3` | Pretest, InformationForm, GetStart | `--surface-2` |
| `--gold` | Pretest, InformationForm, GetStart, Exercise | `--accent` |
| `--gold2` | เหมือนกัน | `--accent2` |
| `--gold3` | เหมือนกัน | `--text` |
| `--shell` | SelectBranch | `--bg` |
| `--card-glass` | SelectBranch | `--surface` |
| `--glass` | SelectBranch | `--surface-2` |
| `--glass-edge` | SelectBranch | `--border` |
| `--brand` | SelectBranch | `--accent` |
| `--brand-fill` `--solid-btn-bg` | SelectBranch | `--accent-fill` |
| `--solid-btn-text` | SelectBranch | `--on-accent` |
| `--field` | SelectBranch | `--tint` |
| `--muted2` `--faint` `--ink-soft` | หลายไฟล์ | `--muted` |
| `--border2` | Pretest, InformationForm | `--border` |
| `--danger` | InformationForm | `--red` |
| `--success` | InformationForm | `--green` |
| `--bdr` | Exercise | `--border` |

**`--navy` คือสีพื้นหลังฟ้าอ่อน ไม่ใช่สีกรมท่า และ `--gold` คือสีน้ำเงิน** — ชื่อพวกนี้เป็นซากจากธีม navy/gold เดิม และมันหลอกคนอ่านโค้ด

---

## 2. ตัวอักษร

### 2.1 ฟอนต์

ฟอนต์มีตัวเดียวคือ `var(--font-sans)` ประกาศที่ `src/index.css`
Noto Sans (ละติน) → IBM Plex Sans Thai Looped (ไทย) → Noto Serif Thai → system-ui

- เขียน `font-family: var(--font-sans)` หรือ `inherit` เท่านั้น **ห้ามพิมพ์ชื่อฟอนต์ตรง ๆ**
- ห้ามเพิ่ม `@import` ฟอนต์ในไฟล์ CSS — โหลดที่ `index.html` ที่เดียว
- โค้ดและตัวเลขใช้ DM Mono / ui-monospace

### 2.2 สเกล

| ชั้น | ขนาด | น้ำหนัก | สี | ใช้กับ |
|---|---|---|---|---|
| title | 22px | 700 | `--text` | หัวข้อหน้า |
| heading | 18px | 700 | `--text` | หัวข้อ modal หัวข้อ section |
| subhead | 16px | 600 | `--ink` | หัวข้อย่อย ชื่อการ์ด |
| body | 15px | 400 | `--ink` | เนื้อความทั่วไป |
| dense | 14px | 400 | `--ink` | เซลล์ตาราง รายการที่แน่น |
| label | 14px | 600 | `--muted` | หัวตาราง label ฟอร์ม |
| caption | 13px | 400 | `--muted` | คำอธิบาย จำนวนรายการ placeholder |

**พื้น 13px — ห้ามมีตัวอักษรเล็กกว่านี้** ปัจจุบันยังมี 12px 56 จุด 11px 41 จุด 10px 24 จุด ต้องไล่เก็บ

### 2.3 กฎภาษาไทย

- **น้ำหนักสูงสุด 700** — IBM Plex Sans Thai Looped มีแค่ 100–700 และ `index.css` ตั้ง `font-synthesis: none` ไว้ ดังนั้น `font-weight: 800` **ไม่มีผล** มันถูกปัดลงเป็น 700 อยู่แล้ว (ตอนนี้เขียน 800 ไว้ 28 จุด เป็นโค้ดที่โกหก)
- **ห้าม `text-transform: uppercase`** ภาษาไทยไม่มีตัวพิมพ์ใหญ่ คำสั่งนี้ไม่ทำอะไรกับไทย แต่ทำให้ข้อความอังกฤษในแถวเดียวกันไม่เข้าพวก
- **ห้าม `letter-spacing` ค่าบวก** กับข้อความที่อาจเป็นไทย เพราะมันดันสระและวรรณยุกต์ออกห่างจากพยัญชนะที่มันเกาะอยู่ ใช้ได้เฉพาะข้อความที่การันตีว่าเป็นละติน เช่น รหัสหรือตัวย่อ
- `line-height` อย่างน้อย **1.5** สำหรับเนื้อความ เพราะไทยมีสระบนและวรรณยุกต์ซ้อนสองชั้น

---

## 3. รัศมีมุม

| token | ค่า | ใช้กับ |
|---|---|---|
| `--r-xs` | `4px` | **เฉพาะ** ชิป `code` แบบ inline ที่แทรกอยู่กลางบรรทัด |
| `--r-sm` | `8px` | ปุ่ม input select badge chip |
| `--r-md` | `12px` | การ์ด modal panel |
| `--r-full` | `9999px` | switch pill progress bar แถบบาง |
| `--r-circle` | `50%` | avatar ไอคอนกลม |

**ห้าค่าเท่านั้น** ตอนนี้ทั้งโปรเจกต์มี 15 ค่า (`Home.css` ไฟล์เดียวมี 15 ค่า) ตาจึงแยกไม่ออกว่าอะไรอยู่ระดับเดียวกัน

`--r-xs` เพิ่มเข้ามาตอนลงมือแก้จริง เพราะ 8px บนชิปสูง 20px กลมจนอ่านยาก **ห้ามใช้กับอย่างอื่น** ส่วนแถบบาง (progress track/fill, scrollbar thumb, slider) ใช้ `--r-full` ไม่ใช่ 2–4px

---

## 4. ระยะ

| token | ค่า | ใช้กับ |
|---|---|---|
| `--s-1` | `4px` | ช่องไฟระหว่างไอคอนกับข้อความ |
| `--s-2` | `8px` | ช่องไฟในแถวเดียวกัน |
| `--s-3` | `12px` | ช่องไฟระหว่าง field |
| `--s-4` | `16px` | ระยะขอบในการ์ด ช่องไฟระหว่างการ์ด |
| `--s-6` | `24px` | ระยะขอบการ์ดใหญ่ ช่องไฟระหว่าง section |
| `--s-8` | `32px` | ระยะขอบหน้า |
| `--s-12` | `48px` | ช่องไฟระหว่าง section ใหญ่ |

ทุกค่าเป็นพหุคูณของ 4 **เลิกใช้ 6 9 10 14 18 22** ซึ่งตอนนี้ปนอยู่เต็มไปหมด (gap `10px` ใช้ 53 จุด)

---

## 5. เส้นขอบและความลึก

ระบบนี้ใช้ **เส้นขอบเป็นตัวบอกระดับ ไม่ใช่เงา**

| token | light | dark | ใช้กับ |
|---|---|---|---|
| `--border` | `#c2d3e0` | `#26344f` | ขอบนอกของการ์ด input ขอบ section |
| `--border-soft` | `#e2e8f0` | `#1f2b42` | เส้นคั่นภายใน เช่น เส้นแบ่งแถวตาราง |

- **ความหนา 1px เท่านั้น** ห้าม 1.5px หรือ 2px
- ขอบนอกเข้มกว่าเส้นคั่นในเสมอ
- **ห้ามใช้ `--bg` วาดเส้น** บนพื้น `--surface` เพราะได้ contrast 1.09:1 คือมองไม่เห็น (เป็นบั๊กที่เพิ่งแก้ไปใน `.ad-table td`)
- **เงาใช้ได้อย่างเดียวคือ modal และ dropdown** ที่ลอยเหนือหน้าจริง ๆ ใช้ `0 8px 24px rgba(15,23,42,.12)` ไม่มีเงาสี ไม่มี glow
- **ห้าม `box-shadow` สีแบรนด์** เช่น `rgba(0,71,171,.4)` หรือ `0 0 40px rgba(16,185,129,.16)` มันคือของประดับ ไม่ได้บอกระดับความลึก

---

## 6. ปุ่มและตัวควบคุม

### 6.1 ปุ่มหลัก

| สถานะ | light | dark |
|---|---|---|
| ปกติ | `--accent-fill` `#0047AB` — ขาว 8.44:1 | `#2f5fd0` — ขาว 5.72:1 |
| hover | `#003A8C` | `#3a6ad9` |
| active | `#002F73` | `#2f5fd0` |
| disabled | พื้น `--muted` ตัวอักษร `--on-accent` `cursor: not-allowed` | เหมือนกัน |

รูปทรง: `--r-sm` 8px สูง 40px ระยะใน `10px 20px` น้ำหนัก 600 ไม่มีขอบ ไม่มีเงา

### 6.2 ปุ่มรอง

พื้น `--surface` ตัวอักษร `--ink` ขอบ 1px `--border` รูปทรงเท่าปุ่มหลัก
hover เปลี่ยนแค่ `border-color` เป็น `--accent` ไม่เปลี่ยนพื้น

### 6.3 กฎ gradient

> **ตัวควบคุมทุกชนิดใช้สีทึบ** — ปุ่ม badge chip tab switch node ของแผนผัง
> gradient ใช้ได้เฉพาะพื้นหลังตกแต่งที่กดไม่ได้ เช่น `.bg` `.bg-grid` `.orb`

เหตุผลไม่ใช่รสนิยม: gradient `--accent` → `--accent2` ทำให้ contrast ของตัวอักษรบนปุ่ม**ไม่คงที่** ปลายซ้ายได้ 8.44:1 ปลายขวาได้ 1.85:1 ตัวหนังสือครึ่งขวาของปุ่มจึงตกมาตรฐานเสมอ

### 6.4 กฎ hover

hover เปลี่ยนได้แค่ **สี** เท่านั้น

ห้าม `transform: translateY(...)` ห้าม `scale()` ห้าม `opacity` เพราะ opacity ลากทั้งกล่องรวมตัวอักษรให้จางลงต่ำกว่า AA

---

## 7. Dark mode

- ธีมมาจาก `PreferencesContext` ซึ่งตั้ง `<html data-theme="dark">`
- ทุกไฟล์ CSS **ต้องมี** บล็อก `:root[data-theme="dark"] <scope>` ที่ประกาศ token ครบทุกตัวที่ไฟล์นั้นใช้
- **สีใหม่ต้องมีสองค่าเสมอ** ห้ามเขียน hex ที่ใช้ได้ธีมเดียว
- ประกาศ token บน scope ของหน้า (`.app` `.ad-app` `.sb2-root`) **ไม่ใช่แค่ `:root`** เพราะหลายไฟล์ตั้งชื่อซ้ำกัน ไฟล์ที่ bundle โหลดทีหลังจะทับของหน้าอื่น
- ตัวช่วยสีใน JS (`getProgressColor` `getNodeColors` `BEHAVIOR_META`) คืนค่าเป็น `var(--…)` จึงต้องใช้ผ่าน `style`/`className` — **attribute ของ SVG อย่าง `fill="var(--x)"` ไม่ resolve ตัวแปร CSS**

**หนี้ที่ต้องแก้:** `InformationForm.css` และ `Pretest.css` ไม่มีบล็อก dark เลย ปุ่มสลับธีมจึงไม่มีผลกับสองหน้านี้

---

## 8. ไอคอน

- ไอคอนทุกตัวมาจาก `react-icons/fa6` **ห้ามใช้อีโมจิ** ในปุ่ม ป้าย avatar เมนู หรือ empty state
- ไอคอนประดับใส่ `aria-hidden`
- ปุ่มที่มีแต่ไอคอนต้องมี `aria-label` หรือ `title`
- `<svg>` ที่กำหนดขนาดด้วย `1em` จะยุบเหลือ 0px เมื่อเป็น flex item ใน Chrome — `.app svg` / `.ad-app svg` ตั้ง `flex-shrink: 0` ไว้แล้ว ต้องคงไว้

---

## 9. โครงหน้า

ทุกหน้าเรียงชั้นเหมือนกัน

```
page header   ← title (22/700) + caption บอกจำนวนหรือบริบท
toolbar       ← ค้นหา / ตัวกรอง / ปุ่มหลักชิดขวา
card          ← เนื้อหาจริง (ตาราง ฟอร์ม รายการ)
modal         ← งานที่ต้องโฟกัสทีละอย่าง
```

- หนึ่งหน้ามี **ปุ่มหลักได้ปุ่มเดียว** ที่เหลือเป็นปุ่มรอง
- ระยะระหว่างชั้น `--s-6` 24px
- ความกว้างเนื้อหาสูงสุด 1280px

---

## 10. Checklist ก่อน commit

- [ ] ไม่มี hex สีในไฟล์ที่แก้ มีแต่ `var(--…)`
- [ ] ไม่มี `font-size` ต่ำกว่า 13px
- [ ] ไม่มี `font-weight` เกิน 700
- [ ] ไม่มี `text-transform: uppercase` หรือ `letter-spacing` ค่าบวกบนข้อความไทย
- [ ] รัศมีเป็น 8 / 12 / 9999px / 50% เท่านั้น
- [ ] ระยะเป็นพหุคูณของ 4
- [ ] ขอบหนา 1px
- [ ] ไม่มี gradient บนสิ่งที่กดได้
- [ ] hover เปลี่ยนแค่สี
- [ ] สีใหม่ทุกตัวมีค่า light และ dark
- [ ] `npx tsc -b` ไม่มี error เพิ่มจาก baseline (ของเดิมมีราว 148 error)

---

## 11. สถานะการย้ายเข้ามาตรฐาน

| ไฟล์ | สถานะ | หมายเหตุ |
|---|---|---|
| `Home.css` | ✅ ผ่าน checklist | token 3 ระดับ · รัศมี 15 → 4 ค่า · gradient 13 → 0 · ตัวอักษรพื้น 13px · เงาเหลือเฉพาะ modal/dropdown |
| `SelectBranch.css` | ✅ ผ่าน checklist | เปลี่ยนชื่อ token ครบ · ย้าย `@import` ฟอนต์ไป `index.html` · เลิกยก/ย่อตอน hover |
| `Pretest.css` | ✅ ผ่าน checklist | **ลบสำเนาซ้ำครึ่งไฟล์** · เพิ่มบล็อก dark · scope ใต้ `.pt-root` · แก้ `#ffff` ที่เป็น hex ผิดรูป |
| `InformationForm.css` | ✅ ผ่าน checklist | เพิ่มบล็อก dark · scope ใต้ `.info-root` · เลิกใช้ `'Playfair Display'` ที่ไม่เคยโหลด |
| `Adminhome.css` | 🟡 บางส่วน | ทำแล้ว: token, ปุ่มหลัก, ตาราง, รัศมีของหน้าแบบฝึกหัด<br>ค้าง: gradient 7 · ตัวอักษร < 13px 27 จุด · น้ำหนัก > 700 8 จุด · ขอบหนาเกิน 8 จุด |
| `Exercise.css` | ⬜ | เปลี่ยนชื่อ token (`--gold*`, `--bdr`) · gradient 9 จุด · `'Playfair Display'` 4 จุด · ต้อง scope (`.btn-next` `.choices` ชนกับ Pretest) |
| `GetStart.css` | ⬜ | เปลี่ยนชื่อ token (`--navy*`, `--gold*`) |
| `signInAndUp.css` | ⬜ | ยังไม่สำรวจ |
| `CodeBlock.css` | ✅ ยกเว้นโดยตั้งใจ | palette Catppuccin ประกาศบน `.ctp-code` เองตามที่ CLAUDE.md กำหนด |

### การ scope ต่อหน้า

ชื่อ class ทั่วไปอย่าง `.page` `.bg` `.orb` `.brand` `.card` `.btn-next` `.choices` ถูกประกาศซ้ำข้ามไฟล์ด้วยค่าที่ต่างกัน และ Vite รวม CSS ทุกไฟล์เป็นก้อนเดียว **ไฟล์ที่ถูกวางทีหลังจึงทับหน้าอื่นโดยไม่ตั้งใจ** — `.btn-next` เคยถูกนิยามต่างกันใน 3 ไฟล์พร้อมกัน

ทุกไฟล์ที่ทำแล้วจึงถูก scope ใต้ class ราก: `.app` (นักศึกษา) · `.ad-app` (admin) · `.sb2-root` · `.pt-root` · `.info-root`
**ไฟล์ใหม่ทุกไฟล์ต้องมี scope ของตัวเอง** และต้องเพิ่ม class รากนั้นที่ element นอกสุดของหน้าใน `.tsx`

### ของที่แก้ไม่ได้ในชั้น CSS

- `style={{...}}` 178 จุดใน 30 ไฟล์ `.tsx` ต้องย้ายเข้า CSS ทีละไฟล์
- `src/component/information/component/SelectBranchModal.tsx` **ไม่มีใคร import — เป็นโค้ดตาย** รอตัดสินใจว่าจะลบหรือเอากลับมาใช้
