# สำคัญที่สุด: dockerfile ทำ COPY --from=deps node_modules ก่อน แล้วค่อย COPY . .
# ถ้าไม่ ignore ตัวนี้ node_modules ของ Windows จะทับ node_modules ที่ build ใน Linux container
node_modules
dist

.git
.gitignore

# ignore เฉพาะ local overrides
# .env กับ .env.production ต้องอยู่ใน build context เพราะ vite build อ่านตอน build time
.env.local
.env.*.local

npm-debug.log*
yarn-error.log*

dockerfile
Dockerfile
.dockerignore
docker-compose*.yaml
docker-compose*.yml

.vscode
.idea
*.md
